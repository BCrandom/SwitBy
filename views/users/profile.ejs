<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil | SwitBy</title>

    <link rel="stylesheet" href="/css/bootstrap.min.css" />

    <link rel="stylesheet" href="main.css">

    <!-- Iconos de Bootstrap (Opcional, para el menú de 3 puntos) -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
</head>
<body>

    <%- include('../partials/header') %>

    <div class="container py-5">
    <div class="row">
      <!-- Sidebar -->
      <div class="col-md-4 mb-4">
        <div class="list-group">
          <a href="/profile/editar/<%= user ? user.id : '' %>" class="list-group-item list-group-item-action active">Editar Perfil</a>
          <a href="#" class="list-group-item list-group-item-action">Cambiar Correo</a>
          <a href="#" class="list-group-item list-group-item-action">Cambiar Contraseña</a>
          <a href="#" class="list-group-item list-group-item-action text-danger">Borrar Cuenta</a>
        </div>
      </div>

      <!-- Perfil -->
        <div class="col-md-8">
            <div class="card shadow-sm">
            <div class="card-body text-center">
                <!-- Avatar -->
                <div class="mb-3">
                    <div class="rounded-circle bg-purple mx-auto" style="width: 100px; height: 100px; background-color: #6f42c1;"></div>
                    </div>
                <!-- Nombre y correo -->
                <h5 class="card-title"><%= user.username %></h5>
                <p class="card-text text-muted"><%= user.email %></p>
                <button class="btn btn-outline-primary btn-sm mb-3"><%= user.first_name + user.last_name %></button>
                <!-- Descripción -->
                <div class="border rounded p-3 text-start">
                <p class="mb-0">
                    <%= user.bio %>
                </p>
                </div>
            </div>
            </div>
        </div>
        </div>
    </div>
    
    <%- include('../partials/footer') %>

    <!-- Scripts unificados -->
    <script src="/js/bootstrap.bundle.min.js"></script>

</body>
</html>