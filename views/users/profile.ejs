<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil | SwitBy</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
</head>
<body>

    <%- include('../partials/header') %>

    <div class="container py-5">
        <div class="row">
            
            <!-- SIDEBAR: Menú de Opciones Realistas -->
            <div class="col-md-4 mb-4">
                <div class="list-group shadow-sm">
                    <!-- Sección 1: Datos Personales (Tabla Perfiles) -->
                    <div class="list-group-item disabled fw-bold text-uppercase small text-muted bg-light">
                        Mi Cuenta
                    </div>
                    <a href="/profile/edit/<%= user.id %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        <div><i class="bi bi-person-gear me-2"></i> Editar Perfil</div>
                    </a>
                    <a href="/profile/segurity/<%= user.id %>" class="list-group-item list-group-item-action">
                        <i class="bi bi-shield-lock me-2"></i> Seguridad (Email/Pass)
                    </a>

                    <!-- Sección 2: Actividad (Relaciones con Publicaciones/Likes) -->
                    <div class="list-group-item disabled fw-bold text-uppercase small text-muted bg-light mt-2">
                        Mi Actividad
                    </div>
                    <a href="/profile/posts" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        <div><i class="bi bi-journal-text me-2"></i> Mis Publicaciones</div>
                        <!-- Badge con número real de posts (puedes pasarlo desde el controlador) -->
                        <span class="badge bg-primary rounded-pill"><%= user.postCount || 0 %></span>
                    </a>
                    <a href="/profile/likes" class="list-group-item list-group-item-action">
                        <i class="bi bi-heart me-2"></i> Mis "Me gusta"
                    </a>

                    <!-- Sección 3: Zona de Peligro -->
                    <div class="list-group-item disabled fw-bold text-uppercase small text-muted bg-light mt-2">
                        Zona de Peligro
                    </div>
                    <a href="/profile/delete/<%= user.id %>" class="list-group-item list-group-item-action text-danger" onclick="return confirm('¿Estás seguro? Esta acción no se puede deshacer.')">
                        <i class="bi bi-trash me-2"></i> Eliminar Cuenta
                    </a>
                </div>
            </div>

            <!-- VISTA PREVIA DEL PERFIL -->
            <div class="col-md-8">
                <div class="card shadow-sm border-0">
                    <!-- Fondo decorativo (Opcional, futuro feature: banner_url) -->
                    <div style="height: 100px; background: linear-gradient(90deg, #6f42c1 0%, #a64dff 100%); border-radius: 5px 5px 0 0;"></div>
                    
                    <div class="card-body text-center" style="margin-top: -50px;">
                        
                        <!-- Avatar Real (Corregido) -->
                        <div class="mb-3">
                            <img 
                                src="<%= user.avatar ? user.avatar : '../imgs/Logo.png' %>"
                                alt="Avatar" 
                                class="rounded-circle border border-4 border-white shadow"
                                style="width: 120px; height: 120px; object-fit: cover; background-color: #fff;"
                                onerror="this.src='../imgs/Logo.png'"
                            >
                        </div>

                        <!-- Datos del Usuario -->
                        <h3 class="card-title fw-bold mb-0"><%= user.username %></h3>
                        <p class="text-muted mb-2"><%= user.email %></p>
                        
                        <div class="mb-3">
                            <% if (user.first_name || user.last_name) { %>
                                <span class="badge bg-light text-dark border px-3 py-2 fs-6">
                                    <i class="bi bi-person-vcard"></i> 
                                    <%= user.first_name %> <%= user.last_name %>
                                </span>
                            <% } else { %>
                                <span class="badge bg-warning text-dark">Perfil incompleto</span>
                            <% } %>
                            
                            <!-- Rol (Opcional, si tienes el dato en sesión) -->
                            <% if (user.role === 'Administrador') { %>
                                <span class="badge bg-danger">Admin</span>
                            <% } %>
                        </div>

                        <!-- Bio / Descripción -->
                        <div class="card bg-light border-0 p-3 text-start mx-auto" style="max-width: 600px;">
                            <h6 class="fw-bold text-muted text-uppercase small mb-2">Biografía</h6>
                            <p class="mb-0 fst-italic text-secondary">
                                <%= user.bio ? user.bio : 'Este usuario aún no ha escrito una biografía.' %>
                            </p>
                        </div>

                        <!-- Estadísticas Rápidas (Recomendación a futuro) -->
                        <div class="row mt-4 border-top pt-3">
                            <div class="col-4">
                                <h5 class="fw-bold m-0">0</h5>
                                <small class="text-muted">Publicaciones</small>
                            </div>
                            <div class="col-4 border-start border-end">
                                <h5 class="fw-bold m-0">0</h5>
                                <small class="text-muted">Seguidores</small>
                            </div>
                            <div class="col-4">
                                <h5 class="fw-bold m-0">0</h5>
                                <small class="text-muted">Siguiendo</small>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer') %>
    <script src="/js/bootstrap.bundle.min.js"></script>

</body>
</html>