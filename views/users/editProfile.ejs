<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SwitBy | Editar Perfil</title>
    
    <!-- Estilos -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/main.css"> <!-- Tus estilos personalizados -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
</head>
<body class="bg-light">

    <%- include('../partials/header') %>

    <div class="container py-5">
        <div class="row justify-content-center">
            
            <div class="col-lg-8">
                <!-- Tarjeta Principal -->
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-white border-bottom py-3">
                        <div class="d-flex justify-content-between align-items-center">
                            <h4 class="mb-0 fw-bold"><i class="bi bi-person-gear me-2"></i>Editar Perfil</h4>
                            <a href="/profile/<%= user.id %>" class="btn btn-sm btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Volver al perfil
                            </a>
                        </div>
                    </div>

                    <div class="card-body p-4">
                        
                        <!-- IMPORTANTE: El action debe apuntar a la ruta que procesa el POST -->
                        <form action="/profile/edit/<%= user.id %>" method="POST">
                            
                            <!-- SECCIÓN 1: AVATAR -->
                            <div class="mb-4 text-center">
                                <label class="form-label fw-bold d-block text-start">Foto de Perfil</label>
                                
                                <div class="d-flex flex-column flex-md-row align-items-center gap-4">
                                    <!-- Previsualización de la imagen actual -->
                                    <img 
                                        src="<%= user.avatar ? user.avatar : '../../imgs/Logo.png' %>" 
                                        alt="Avatar Preview" 
                                        class="rounded-circle border"
                                        style="width: 100px; height: 100px; object-fit: cover;"
                                        onerror="this.src='../../imgs/Logo.png'"
                                    >
                                    
                                    <div class="w-100">
                                        <!-- Input para URL de imagen (Acorde a tu base de datos actual: avatar_url TEXT) -->
                                        <!-- Nota: A futuro esto se reemplaza por un input type="file" -->
                                        <div class="input-group">
                                            <span class="input-group-text"><i class="bi bi-link-45deg"></i></span>
                                            <input 
                                                type="url" 
                                                class="form-control" 
                                                name="avatar_url" 
                                                placeholder="https://ejemplo.com/mifoto.jpg"
                                                value="<%= user.avatar || '' %>"
                                            >
                                        </div>
                                        <div class="form-text">Pega aquí la URL de tu imagen de perfil.</div>
                                    </div>
                                </div>
                            </div>

                            <hr class="my-4">

                            <!-- SECCIÓN 2: INFORMACIÓN PÚBLICA -->
                            <h5 class="mb-3 text-muted">Información Pública</h5>

                            <!-- Nombre de Usuario -->
                            <div class="mb-3">
                                <label for="username" class="form-label fw-bold">Nombre de usuario</label>
                                <div class="input-group">
                                    <span class="input-group-text">@</span>
                                    <input 
                                        type="text" 
                                        class="form-control" 
                                        id="username" 
                                        name="username" 
                                        value="<%= user.username %>" 
                                        required
                                    >
                                </div>
                                <div class="form-text">Este es el nombre que verán los demás usuarios.</div>
                            </div>

                            <!-- Bio -->
                            <div class="mb-3">
                                <label for="bio" class="form-label fw-bold">Biografía</label>
                                <!-- NOTA: En textareas, el valor va DENTRO de las etiquetas, no en 'value' -->
                                <textarea 
                                    class="form-control" 
                                    id="bio" 
                                    name="bio" 
                                    rows="4" 
                                    maxlength="250"
                                    placeholder="Cuéntanos un poco sobre ti..."
                                ><%= user.bio || '' %></textarea>
                                <div class="form-text text-end">Máximo 250 caracteres.</div>
                            </div>

                            <hr class="my-4">

                            <!-- SECCIÓN 3: INFORMACIÓN PERSONAL -->
                            <h5 class="mb-3 text-muted">Datos Personales</h5>
                            
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label for="first_name" class="form-label fw-bold">Nombre</label>
                                    <input 
                                        type="text" 
                                        class="form-control" 
                                        id="first_name" 
                                        name="first_name" 
                                        value="<%= user.first_name || '' %>"
                                    >
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label for="last_name" class="form-label fw-bold">Apellido</label>
                                    <input 
                                        type="text" 
                                        class="form-control" 
                                        id="last_name" 
                                        name="last_name" 
                                        value="<%= user.last_name || '' %>"
                                    >
                                </div>
                            </div>

                            <!-- Botones de Acción -->
                            <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                                <a href="/profile/<%= user.id %>" class="btn btn-outline-secondary me-md-2">Cancelar</a>
                                <button type="submit" class="btn btn-primary fw-bold px-4">
                                    <i class="bi bi-save me-2"></i> Guardar Cambios
                                </button>
                            </div>

                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="/js/bootstrap.bundle.min.js"></script>
    
</body>
</html>