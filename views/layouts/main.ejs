<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SwitBy | Home</title>
    <!-- Estilos: Intenta unificar o usar rutas absolutas -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    
    <link rel="stylesheet" href="main.css">

    <!-- Iconos de Bootstrap (Opcional, para el menú de 3 puntos) -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />
  </head>
  <body>
    <%- include('../partials/header') %>

    <!-- Barra de Herramientas -->
    <section
      id="perfil-publicaciones"
      class="d-flex justify-content-between align-items-center p-3 border-bottom bg-white sticky-top"
    >
      <form class="d-flex" role="search">
        <button class="btn btn-outline-dark fw-bold" type="submit">
          Buscar
        </button>
      </form>

      <div class="d-flex gap-2 align-items-center">
        <a href="/createPost" class="btn btn-dark fw-bold">
          <i class="bi bi-plus-lg"></i> Crear Post
        </a>

        <!-- Enlace dinámico al perfil del usuario logueado -->
        <a
          href="/profile/<%= user ? user.id : '' %>"
          class="btn btn-outline-dark fw-bold d-flex align-items-center gap-2"
        > <!-- <%= user ? user.id : '' %> -->
          <!-- Mini avatar del usuario en sesión -->
          <img
            src="<%= user && user.avatar ? user.avatar : 'imgs/Logo.png' %>"
            alt="m"
            width="24"
            height="24"
            class="rounded-circle border"
          />
          Perfil
        </a>
      </div>
    </section>

    <div id="tuperfil" class="container-fluid">
      <div class="row min-vh-100">
        <!-- Sidebar Izquierdo -->
        <section
          id="perfil-opc"
          class="col-md-3 d-none d-md-block border-end p-0 bg-white"
        >
          <div class="list-group list-group-flush pt-3">
            <a href="#" class="list-group-item list-group-item-action fw-bold">
              <i class="bi bi-people"></i> Amigos</a
            >
            <a href="#" class="list-group-item list-group-item-action fw-bold">
              <i class="bi bi-tags"></i> Categorías</a
            >
            <a href="#" class="list-group-item list-group-item-action fw-bold">
              <i class="bi bi-tags"></i> Temas</a
            >
            <a href="#" class="list-group-item list-group-item-action fw-bold">
              <i class="bi bi-chat-left-text"></i> Mensajes</a
            >
          </div>
        </section>

        <!-- Feed Principal -->
        <section id="perfil-feed" class="col-12 col-md-9 p-4 bg-light">
          <h4 class="mb-4 text-muted">Tu Feed</h4>

          <% if (receivePosts && receivePosts.length > 0) { %> 
          <% receivePosts.forEach(function(post) { %>

          <div class="card mb-4 shadow-sm border-0">
            <div class="card-body">
              <!-- Cabecera del Post: Avatar, Nombre y Menú -->
              <div
                class="d-flex justify-content-between align-items-start mb-3"
              >
                <div class="d-flex align-items-center gap-3">
                  <!-- Avatar del Autor del Post (Viene del JOIN con perfiles) -->
                  <img
                    src="<%= post.author_avatar ? post.author_avatar : 'imgs/Logo.png' %>"
                    alt="Avatar"
                    class="rounded-circle border"
                    width="50"
                    height="50"
                    style="object-fit: cover"
                  />

                  <div>
                    <h5 class="card-title mb-0 fw-bold text-dark">
                      <%= post.author_name %>
                      <!-- Usamos el nuevo alias author_name -->
                    </h5>
                    <small class="text-muted" style="font-size: 0.8rem">
                      <%= post.creationDate %>
                    </small>
                  </div>
                </div>

                <!-- Menú de Opciones (Solo si es dueño) -->
                <% if (user && post.user_id == user.id) { %>
                <div class="dropdown">
                  <button
                    class="btn btn-link text-dark text-decoration-none p-0"
                    type="button"
                    data-bs-toggle="dropdown"
                    aria-expanded="false"
                  >
                    <i class="bi bi-three-dots-vertical fs-5"></i>
                  </button>
                  <ul class="dropdown-menu dropdown-menu-end shadow">
                    <li>
                      <a
                        class="dropdown-item"
                        href="/post/edit/<%= post.post_id %>"
                        >Editar</a
                      >
                    </li>
                    <li><hr class="dropdown-divider" /></li>
                    <li>
                      <!-- Formulario para eliminar (mejor que un simple link por seguridad) -->
                      <form
                        action="/post/delete/<%= post.post_id %>"
                        method="POST"
                        onsubmit="return confirm('¿Borrar post?');"
                      >
                        <button type="submit" class="dropdown-item text-danger">
                          Eliminar
                        </button>
                      </form>
                    </li>
                  </ul>
                </div>
                <% } %>
              </div>

              <!-- Contenido del Post -->
              <h3 class="h5 fw-bold mb-2"><%= post.title %></h3>

              <!-- Badge de Categoría (Viene del LEFT JOIN con categorias) -->
              <% if (post.category_name) { %>
              <span class="badge bg-secondary mb-2"
                ><%= post.category_name %></span
              >
              <% } %>

              <p class="card-text mt-2 text-secondary"><%= post.content %></p>
            </div>

            <!-- Pie del Post (Likes y Comentarios - Estructura futura) -->
            <div class="card-footer bg-white border-0 d-flex gap-3">
              <button class="btn btn-sm btn-outline-danger border-0">
                <i class="bi bi-heart"></i> Like
              </button>
              <button class="btn btn-sm btn-outline-primary border-0">
                <i class="bi bi-chat"></i> Comentar
              </button>
            </div>
          </div>

          <% }); %> <% } else { %>

          <!-- Estado Vacío -->
          <div class="text-center py-5">
            <div class="card-body">
              <i class="bi bi-journal-x display-1 text-muted"></i>
              <h5 class="card-title mt-3">¡No hay publicaciones aún!</h5>
              <p class="card-text text-muted">
                Sé el primero en compartir algo con la comunidad.
              </p>
              <a href="/createPost" class="btn btn-primary mt-2"
                >Crear publicación</a
              >
            </div>
          </div>

          <% } %>
        </section>
      </div>
    </div>

    <%- include('../partials/footer') %>

    <!-- Scripts unificados -->
    <script src="/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
